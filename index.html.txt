<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ibrahim — Creative Developer</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=Space+Mono:wght@400;700&display=swap');

*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

:root {
–bg: #050508;
–gold: #c9a84c;
–gold2: #f0d080;
–white: #f0ece4;
–grey: #888;
–card: #0d0d14;
}

html { scroll-behavior: smooth; }

body {
background: var(–bg);
color: var(–white);
font-family: ‘Syne’, sans-serif;
overflow-x: hidden;
cursor: none;
}

/* CURSOR */
#cursor {
width: 12px; height: 12px;
background: var(–gold);
border-radius: 50%;
position: fixed; top: 0; left: 0;
pointer-events: none; z-index: 9999;
transform: translate(-50%,-50%);
transition: transform 0.1s, width 0.2s, height 0.2s, background 0.2s;
}
#cursor-ring {
width: 36px; height: 36px;
border: 1px solid rgba(201,168,76,0.5);
border-radius: 50%;
position: fixed; top: 0; left: 0;
pointer-events: none; z-index: 9998;
transform: translate(-50%,-50%);
transition: left 0.12s ease-out, top 0.12s ease-out;
}
body:has(a:hover) #cursor, body:has(button:hover) #cursor { width: 20px; height: 20px; background: var(–gold2); }

/* NAV */
nav {
position: fixed; top: 0; left: 0; right: 0; z-index: 100;
display: flex; justify-content: space-between; align-items: center;
padding: 28px 60px;
background: linear-gradient(to bottom, rgba(5,5,8,0.95), transparent);
}
.nav-logo {
font-size: 1.1rem; font-weight: 800; letter-spacing: 0.15em;
color: var(–gold); text-transform: uppercase;
}
.nav-links { display: flex; gap: 40px; }
.nav-links a {
color: var(–grey); text-decoration: none; font-size: 0.75rem;
letter-spacing: 0.15em; text-transform: uppercase;
transition: color 0.3s;
}
.nav-links a:hover { color: var(–white); }

/* HERO */
#hero {
height: 100vh;
display: flex; align-items: center; justify-content: center;
position: relative; overflow: hidden;
}
#hero-canvas {
position: absolute; inset: 0; width: 100%; height: 100%;
}
.hero-content {
position: relative; z-index: 2;
text-align: center;
}
.hero-tag {
font-family: ‘Space Mono’, monospace;
font-size: 0.7rem; letter-spacing: 0.25em;
color: var(–gold); text-transform: uppercase;
margin-bottom: 20px;
opacity: 0; animation: fadeUp 0.8s 0.3s forwards;
}
.hero-name {
font-size: clamp(4rem, 10vw, 9rem);
font-weight: 800; line-height: 0.95;
letter-spacing: -0.03em;
background: linear-gradient(135deg, #f0ece4 0%, #c9a84c 50%, #f0ece4 100%);
-webkit-background-clip: text; -webkit-text-fill-color: transparent;
background-clip: text;
opacity: 0; animation: fadeUp 0.8s 0.5s forwards;
}
.hero-sub {
font-size: 1rem; color: var(–grey); margin-top: 16px;
letter-spacing: 0.08em;
opacity: 0; animation: fadeUp 0.8s 0.7s forwards;
}
.hero-cta {
margin-top: 48px;
opacity: 0; animation: fadeUp 0.8s 0.9s forwards;
}
.btn {
display: inline-block; padding: 14px 40px;
border: 1px solid var(–gold); color: var(–gold);
text-decoration: none; font-size: 0.75rem; letter-spacing: 0.2em;
text-transform: uppercase; cursor: none;
transition: background 0.3s, color 0.3s;
}
.btn:hover { background: var(–gold); color: var(–bg); }

.scroll-hint {
position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%);
font-family: ‘Space Mono’, monospace; font-size: 0.65rem;
letter-spacing: 0.2em; color: var(–grey); text-transform: uppercase;
opacity: 0; animation: fadeUp 0.8s 1.2s forwards;
display: flex; flex-direction: column; align-items: center; gap: 10px;
}
.scroll-line {
width: 1px; height: 50px; background: linear-gradient(to bottom, var(–gold), transparent);
animation: scrollLine 2s ease-in-out infinite;
}
@keyframes scrollLine { 0%,100%{opacity:1;transform:scaleY(1)} 50%{opacity:0.3;transform:scaleY(0.5)} }

/* SECTION BASE */
section { padding: 120px 60px; max-width: 1200px; margin: 0 auto; }
.section-label {
font-family: ‘Space Mono’, monospace;
font-size: 0.65rem; letter-spacing: 0.3em; color: var(–gold);
text-transform: uppercase; margin-bottom: 16px;
}
.section-title {
font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 800;
line-height: 1.05; letter-spacing: -0.02em;
margin-bottom: 60px;
}

/* ABOUT */
#about { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
#about .left {}
#about .right { color: var(–grey); line-height: 1.9; font-size: 1rem; }
#about .right strong { color: var(–white); }
.about-stats { display: flex; gap: 40px; margin-top: 40px; }
.stat { }
.stat-num { font-size: 2.5rem; font-weight: 800; color: var(–gold); }
.stat-label { font-size: 0.7rem; letter-spacing: 0.15em; color: var(–grey); text-transform: uppercase; margin-top: 4px; }

/* PROJECTS */
#projects { max-width: 1200px; }
.projects-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2px; }
.project-card {
position: relative; overflow: hidden;
background: var(–card);
aspect-ratio: 4/3;
cursor: none;
}
.project-card canvas { width: 100%; height: 100%; display: block; }
.project-overlay {
position: absolute; inset: 0;
background: linear-gradient(to top, rgba(5,5,8,0.95) 0%, transparent 60%);
padding: 30px;
display: flex; flex-direction: column; justify-content: flex-end;
transform: translateY(20px); opacity: 0.7;
transition: all 0.4s ease;
}
.project-card:hover .project-overlay { transform: translateY(0); opacity: 1; }
.project-tag {
font-family: ‘Space Mono’, monospace;
font-size: 0.6rem; letter-spacing: 0.2em; color: var(–gold);
text-transform: uppercase; margin-bottom: 8px;
}
.project-title { font-size: 1.4rem; font-weight: 700; margin-bottom: 8px; }
.project-desc { font-size: 0.8rem; color: var(–grey); line-height: 1.6; }
.project-card:nth-child(1) { grid-column: span 2; aspect-ratio: 16/6; }

/* SKILLS */
#skills { background: var(–bg); }
.skills-row { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 0; }
.skill-pill {
padding: 10px 22px;
border: 1px solid rgba(201,168,76,0.2);
font-size: 0.75rem; letter-spacing: 0.1em;
color: var(–grey); text-transform: uppercase;
transition: all 0.3s;
}
.skill-pill:hover { border-color: var(–gold); color: var(–gold); }

/* CONTACT */
#contact { text-align: center; padding-bottom: 160px; }
#contact .section-title { margin-bottom: 20px; }
#contact p { color: var(–grey); margin-bottom: 48px; font-size: 1rem; line-height: 1.8; }
.contact-email {
font-family: ‘Space Mono’, monospace;
font-size: 1.1rem; color: var(–gold);
text-decoration: none; border-bottom: 1px solid rgba(201,168,76,0.3);
padding-bottom: 4px; transition: border-color 0.3s;
cursor: none;
}
.contact-email:hover { border-color: var(–gold); }

/* FOOTER */
footer {
border-top: 1px solid rgba(255,255,255,0.05);
padding: 30px 60px;
display: flex; justify-content: space-between; align-items: center;
font-family: ‘Space Mono’, monospace;
font-size: 0.65rem; letter-spacing: 0.1em; color: var(–grey);
text-transform: uppercase;
}

@keyframes fadeUp {
from { opacity: 0; transform: translateY(30px); }
to { opacity: 1; transform: translateY(0); }
}

/* Divider line */
.divider {
width: 100%; height: 1px;
background: linear-gradient(to right, transparent, rgba(201,168,76,0.3), transparent);
margin: 0 60px; width: calc(100% - 120px);
}
</style>

</head>
<body>

<div id="cursor"></div>
<div id="cursor-ring"></div>

<nav>
  <div class="nav-logo">Ibrahim.dev</div>
  <div class="nav-links">
    <a href="#about">About</a>
    <a href="#projects">Work</a>
    <a href="#skills">Skills</a>
    <a href="#contact">Contact</a>
  </div>
</nav>

<!-- HERO -->

<section id="hero">
  <canvas id="hero-canvas"></canvas>
  <div class="hero-content">
    <div class="hero-tag">Creative 3D Developer</div>
    <div class="hero-name">Ibrahim</div>
    <div class="hero-sub">Interactive Experiences · WebGL · 3D Design</div>
    <div class="hero-cta">
      <a href="#projects" class="btn" onclick="document.getElementById('projects').scrollIntoView({behavior:'smooth'});return false;">View Work</a>
    </div>
  </div>
  <div class="scroll-hint">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<div class="divider"></div>

<!-- ABOUT -->

<section id="about">
  <div class="left">
    <div class="section-label">About</div>
    <div class="section-title">Building worlds<br>in the browser.</div>
    <div class="about-stats">
      <div class="stat">
        <div class="stat-num">4+</div>
        <div class="stat-label">Years Experience</div>
      </div>
      <div class="stat">
        <div class="stat-num">20+</div>
        <div class="stat-label">Projects Built</div>
      </div>
      <div class="stat">
        <div class="stat-num">3</div>
        <div class="stat-label">Countries Reached</div>
      </div>
    </div>
  </div>
  <div class="right">
    <p>I'm a <strong>Creative Developer</strong> specializing in high-end 3D web experiences. I combine technical depth with design sensibility to build interactive digital worlds that feel alive.</p>
    <br>
    <p>My toolkit spans <strong>Three.js, WebGL, Spline, and GSAP</strong> — crafting everything from immersive product showcases to real-time 3D configurators. Based in <strong>Dubai, UAE</strong>, working globally.</p>
    <br>
    <p>Every project starts with one question: <strong>how do we make someone stop scrolling?</strong></p>
  </div>
</section>

<div class="divider"></div>

<!-- PROJECTS -->

<section id="projects" style="max-width:100%;padding:120px 0;">
  <div style="padding:0 60px;max-width:1200px;margin:0 auto;">
    <div class="section-label">Selected Work</div>
    <div class="section-title">Projects that<br>push the web forward.</div>
  </div>
  <div class="projects-grid" style="max-width:1200px;margin:0 auto 0;padding:0 60px;">

```
<!-- Project 1 - Featured -->
<div class="project-card" id="proj1">
  <canvas id="c1"></canvas>
  <div class="project-overlay">
    <div class="project-tag">WebGL · Three.js · 2024</div>
    <div class="project-title">NexaSphere — 3D Product Configurator</div>
    <div class="project-desc">Real-time 3D product visualization with dynamic material switching, lighting control, and AR preview export. Built for a luxury watch brand.</div>
  </div>
</div>

<!-- Project 2 -->
<div class="project-card" id="proj2">
  <canvas id="c2"></canvas>
  <div class="project-overlay">
    <div class="project-tag">Spline · GSAP · 2024</div>
    <div class="project-title">Ether — Immersive Landing Page</div>
    <div class="project-desc">Scroll-driven 3D scene for a Web3 startup. Particles, morphing geometry and fluid transitions synced to scroll position.</div>
  </div>
</div>

<!-- Project 3 -->
<div class="project-card" id="proj3">
  <canvas id="c3"></canvas>
  <div class="project-overlay">
    <div class="project-tag">WebGL · Custom Shaders · 2023</div>
    <div class="project-title">Vertex — Interactive Data Globe</div>
    <div class="project-desc">Real-time data visualization on a 3D globe. Arcs, nodes, and live data feeds rendered with custom GLSL shaders.</div>
  </div>
</div>
```

  </div>
</section>

<div class="divider"></div>

<!-- SKILLS -->

<section id="skills">
  <div class="section-label">Expertise</div>
  <div class="section-title">Tools &amp; Technologies</div>
  <div class="skills-row">
    <div class="skill-pill">Three.js</div>
    <div class="skill-pill">WebGL</div>
    <div class="skill-pill">GLSL Shaders</div>
    <div class="skill-pill">Spline</div>
    <div class="skill-pill">GSAP</div>
    <div class="skill-pill">React Three Fiber</div>
    <div class="skill-pill">Blender</div>
    <div class="skill-pill">JavaScript (ES6+)</div>
    <div class="skill-pill">TypeScript</div>
    <div class="skill-pill">HTML / CSS</div>
    <div class="skill-pill">UX / UI Design</div>
    <div class="skill-pill">Figma</div>
    <div class="skill-pill">Motion Design</div>
    <div class="skill-pill">3D Modelling</div>
    <div class="skill-pill">AR / WebXR</div>
  </div>
</section>

<div class="divider"></div>

<!-- CONTACT -->

<section id="contact">
  <div class="section-label">Let's Work</div>
  <div class="section-title">Got a project<br>in mind?</div>
  <p>I'm open to freelance projects, full-time roles,<br>and creative collaborations. Based in Dubai — available immediately.</p>
  <a href="mailto:ik29167@gmail.com" class="contact-email">ik29167@gmail.com</a>
</section>

<footer>
  <span>© 2024 Ibrahim</span>
  <span>Dubai, UAE</span>
  <span>Creative Developer</span>
</footer>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

<script>
// ---- CURSOR ----
const cursor = document.getElementById('cursor');
const ring = document.getElementById('cursor-ring');
document.addEventListener('mousemove', e => {
  cursor.style.left = e.clientX + 'px';
  cursor.style.top = e.clientY + 'px';
  ring.style.left = e.clientX + 'px';
  ring.style.top = e.clientY + 'px';
});

// ---- HERO CANVAS - Floating particles + rotating torus ----
(function(){
  const canvas = document.getElementById('hero-canvas');
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(75, window.innerWidth/window.innerHeight, 0.1, 1000);
  const renderer = new THREE.WebGLRenderer({canvas, antialias:true, alpha:true});
  renderer.setSize(window.innerWidth, window.innerHeight);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));

  camera.position.z = 5;

  // Torus knot
  const geo = new THREE.TorusKnotGeometry(1.2, 0.35, 200, 32);
  const mat = new THREE.MeshStandardMaterial({
    color: 0xc9a84c, metalness: 0.9, roughness: 0.1,
    wireframe: false
  });
  const knot = new THREE.Mesh(geo, mat);
  scene.add(knot);

  // Wireframe overlay
  const wireMat = new THREE.MeshBasicMaterial({color:0xf0d080, wireframe:true, opacity:0.08, transparent:true});
  const wireKnot = new THREE.Mesh(geo, wireMat);
  scene.add(wireKnot);

  // Particles
  const pCount = 1800;
  const pGeo = new THREE.BufferGeometry();
  const positions = new Float32Array(pCount * 3);
  for(let i=0;i<pCount*3;i++) positions[i] = (Math.random()-0.5)*20;
  pGeo.setAttribute('position', new THREE.BufferAttribute(positions,3));
  const pMat = new THREE.PointsMaterial({color:0xc9a84c, size:0.02, transparent:true, opacity:0.4});
  const particles = new THREE.Points(pGeo, pMat);
  scene.add(particles);

  // Lights
  const ambLight = new THREE.AmbientLight(0xffffff, 0.3);
  scene.add(ambLight);
  const pointLight = new THREE.PointLight(0xc9a84c, 3, 20);
  pointLight.position.set(4, 4, 4);
  scene.add(pointLight);
  const pointLight2 = new THREE.PointLight(0x4488ff, 1.5, 20);
  pointLight2.position.set(-4, -2, 2);
  scene.add(pointLight2);

  let mouseX = 0, mouseY = 0;
  document.addEventListener('mousemove', e => {
    mouseX = (e.clientX / window.innerWidth - 0.5) * 2;
    mouseY = -(e.clientY / window.innerHeight - 0.5) * 2;
  });

  let t = 0;
  function animate(){
    requestAnimationFrame(animate);
    t += 0.008;
    knot.rotation.x = t * 0.4 + mouseY * 0.3;
    knot.rotation.y = t * 0.6 + mouseX * 0.3;
    wireKnot.rotation.x = knot.rotation.x;
    wireKnot.rotation.y = knot.rotation.y;
    particles.rotation.y = t * 0.05;
    particles.rotation.x = t * 0.02;
    renderer.render(scene, camera);
  }
  animate();

  window.addEventListener('resize', () => {
    camera.aspect = window.innerWidth/window.innerHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
  });
})();

// ---- PROJECT CARD 1 - Rotating Sphere with Grid ----
(function(){
  const canvas = document.getElementById('c1');
  const w = canvas.parentElement.offsetWidth;
  const h = canvas.parentElement.offsetHeight;
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60, w/h, 0.1, 100);
  camera.position.z = 3.5;
  const renderer = new THREE.WebGLRenderer({canvas, antialias:true, alpha:true});
  renderer.setSize(w, h);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));

  const geo = new THREE.IcosahedronGeometry(1.2, 5);
  const mat = new THREE.MeshStandardMaterial({color:0xc9a84c, metalness:1, roughness:0.05, wireframe:false});
  const sphere = new THREE.Mesh(geo, mat);
  scene.add(sphere);
  const wMat = new THREE.MeshBasicMaterial({color:0xf0d080, wireframe:true, transparent:true, opacity:0.15});
  const wSphere = new THREE.Mesh(new THREE.IcosahedronGeometry(1.22, 3), wMat);
  scene.add(wSphere);

  scene.add(new THREE.AmbientLight(0xffffff,0.4));
  const pl = new THREE.PointLight(0xffd700,4,10); pl.position.set(3,3,3); scene.add(pl);
  const pl2 = new THREE.PointLight(0x0044ff,2,10); pl2.position.set(-3,-2,1); scene.add(pl2);

  let mx=0,my=0;
  canvas.parentElement.addEventListener('mousemove',e=>{
    const r=canvas.getBoundingClientRect();
    mx=(e.clientX-r.left)/r.width*2-1;
    my=-((e.clientY-r.top)/r.height)*2+1;
  });

  let t=0;
  function animate(){requestAnimationFrame(animate);t+=0.006;
    sphere.rotation.y=t+mx*0.5; sphere.rotation.x=my*0.3;
    wSphere.rotation.y=-t*0.7; wSphere.rotation.x=t*0.3;
    renderer.render(scene,camera);}
  animate();
})();

// ---- PROJECT CARD 2 - Particle Cloud ----
(function(){
  const canvas = document.getElementById('c2');
  const w = canvas.parentElement.offsetWidth;
  const h = canvas.parentElement.offsetHeight;
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60,w/h,0.1,100);
  camera.position.z = 4;
  const renderer = new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});
  renderer.setSize(w,h);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));

  const count = 3000;
  const geo = new THREE.BufferGeometry();
  const pos = new Float32Array(count*3);
  const col = new Float32Array(count*3);
  for(let i=0;i<count;i++){
    const r=1.5+Math.random()*1.5;
    const theta=Math.random()*Math.PI*2;
    const phi=Math.acos(2*Math.random()-1);
    pos[i*3]=r*Math.sin(phi)*Math.cos(theta);
    pos[i*3+1]=r*Math.sin(phi)*Math.sin(theta);
    pos[i*3+2]=r*Math.cos(phi);
    const g=0.5+Math.random()*0.5;
    col[i*3]=0.8*g; col[i*3+1]=0.65*g; col[i*3+2]=0.3*g;
  }
  geo.setAttribute('position',new THREE.BufferAttribute(pos,3));
  geo.setAttribute('color',new THREE.BufferAttribute(col,3));
  const mat = new THREE.PointsMaterial({size:0.025,vertexColors:true,transparent:true,opacity:0.85});
  scene.add(new THREE.Points(geo,mat));

  // Inner torus
  const tGeo = new THREE.TorusGeometry(1,0.02,8,100);
  const tMat = new THREE.MeshBasicMaterial({color:0xc9a84c,transparent:true,opacity:0.3});
  const torus = new THREE.Mesh(tGeo,tMat); scene.add(torus);
  const torus2 = new THREE.Mesh(new THREE.TorusGeometry(0.7,0.015,8,80),tMat.clone()); scene.add(torus2);

  let mx=0,my=0;
  canvas.parentElement.addEventListener('mousemove',e=>{
    const r=canvas.getBoundingClientRect();
    mx=(e.clientX-r.left)/r.width*2-1;
    my=-((e.clientY-r.top)/r.height)*2+1;
  });

  let t=0;
  function animate(){requestAnimationFrame(animate);t+=0.006;
    const pts = geo.attributes.position.array;
    for(let i=0;i<count;i++){
      pts[i*3+1]+=Math.sin(t+i*0.1)*0.0008;
    }
    geo.attributes.position.needsUpdate=true;
    torus.rotation.x=t*0.5+my*0.3; torus.rotation.y=t*0.3+mx*0.3;
    torus2.rotation.x=-t*0.4; torus2.rotation.z=t*0.6;
    renderer.render(scene,camera);}
  animate();
})();

// ---- PROJECT CARD 3 - Globe with arcs ----
(function(){
  const canvas = document.getElementById('c3');
  const w = canvas.parentElement.offsetWidth;
  const h = canvas.parentElement.offsetHeight;
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60,w/h,0.1,100);
  camera.position.z = 3.5;
  const renderer = new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});
  renderer.setSize(w,h);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));

  // Globe wireframe
  const gGeo = new THREE.SphereGeometry(1.3,32,32);
  const gMat = new THREE.MeshBasicMaterial({color:0xc9a84c,wireframe:true,transparent:true,opacity:0.12});
  const globe = new THREE.Mesh(gGeo,gMat); scene.add(globe);

  // Solid inner
  const gMat2 = new THREE.MeshStandardMaterial({color:0x0a0a14,metalness:0.5,roughness:0.5});
  const globe2 = new THREE.Mesh(new THREE.SphereGeometry(1.28,32,32),gMat2); scene.add(globe2);

  // Glowing dots on surface
  const dotCount = 60;
  for(let i=0;i<dotCount;i++){
    const phi=Math.acos(2*Math.random()-1);
    const theta=Math.random()*Math.PI*2;
    const r=1.32;
    const dot = new THREE.Mesh(
      new THREE.SphereGeometry(0.025,8,8),
      new THREE.MeshBasicMaterial({color: Math.random()>0.5 ? 0xc9a84c : 0x4488ff})
    );
    dot.position.set(r*Math.sin(phi)*Math.cos(theta),r*Math.sin(phi)*Math.sin(theta),r*Math.cos(phi));
    scene.add(dot);
  }

  // Arc lines
  for(let i=0;i<12;i++){
    const pts=[];
    const phi1=Math.acos(2*Math.random()-1); const theta1=Math.random()*Math.PI*2;
    const phi2=Math.acos(2*Math.random()-1); const theta2=Math.random()*Math.PI*2;
    const r=1.32;
    const p1=new THREE.Vector3(r*Math.sin(phi1)*Math.cos(theta1),r*Math.sin(phi1)*Math.sin(theta1),r*Math.cos(phi1));
    const p2=new THREE.Vector3(r*Math.sin(phi2)*Math.cos(theta2),r*Math.sin(phi2)*Math.sin(theta2),r*Math.cos(phi2));
    const mid=p1.clone().add(p2).multiplyScalar(0.5).normalize().multiplyScalar(r*1.4);
    for(let j=0;j<=30;j++){
      const t=j/30;
      const p=new THREE.Vector3().lerpVectors(p1,mid,t).lerp(new THREE.Vector3().lerpVectors(mid,p2,t),t);
      pts.push(p);
    }
    const arcGeo=new THREE.BufferGeometry().setFromPoints(pts);
    const arcMat=new THREE.LineBasicMaterial({color:0xc9a84c,transparent:true,opacity:0.25+Math.random()*0.2});
    scene.add(new THREE.Line(arcGeo,arcMat));
  }

  scene.add(new THREE.AmbientLight(0xffffff,0.5));
  const pl=new THREE.PointLight(0xc9a84c,3,10); pl.position.set(3,3,3); scene.add(pl);

  let mx=0,my=0;
  canvas.parentElement.addEventListener('mousemove',e=>{
    const r=canvas.getBoundingClientRect();
    mx=(e.clientX-r.left)/r.width*2-1;
    my=-((e.clientY-r.top)/r.height)*2+1;
  });

  let t=0;
  function animate(){requestAnimationFrame(animate);t+=0.005;
    globe.rotation.y=t+mx*0.4; globe.rotation.x=my*0.2;
    globe2.rotation.y=t+mx*0.4; globe2.rotation.x=my*0.2;
    renderer.render(scene,camera);}
  animate();
})();
</script>

</body>
</html>